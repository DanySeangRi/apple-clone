import React from 'react'

const BigCard = ({ image }) => {
  return (
    <div className='h-173 bg-cover bg-center mb-4' style={{ backgroundImage: `url(${image})` }}>
     
    </div>
  )
}

export default BigCard
